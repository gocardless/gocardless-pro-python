# WARNING: Do not edit by hand, this file was generated by Crank:
#
#   https://github.com/gocardless/crank
#

from . import base_service
from .. import resources
from ..paginator import Paginator
from .. import errors

class PaymentAccountTransactionsService(base_service.BaseService):
    """Service class that provides access to the payment_account_transactions
    endpoints of the GoCardless Pro API.
    """

    RESOURCE_CLASS = resources.PaymentAccountTransaction
    RESOURCE_NAME = 'payment_account_transactions'


    def list(self,identity,params=None, headers=None):
        """List payment account transactions.

        List transactions for a given payment account.

        Args:
              identity (string): The unique ID of the [bank account](#core-endpoints-creditor-bank-accounts) which happens to be the payment account.
              params (dict, optional): Query string parameters.

        Returns:
              ListResponse of PaymentAccountTransaction instances
        """
        path = self._sub_url_params('/payment_accounts/:identity/transactions', {
          
            'identity': identity,
          })
        

        response = self._perform_request('GET', path, params, headers,
                                         retry_failures=True)
        return self._resource_for(response)

    def all(self,identity,params=None):
        if params is None:
            params = {}
        return Paginator(self, params, identity_params={
          
            'identity': identity,
          })
    
  
