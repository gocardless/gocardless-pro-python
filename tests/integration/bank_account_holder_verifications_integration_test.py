# WARNING: Do not edit by hand, this file was generated by Crank:
#
#   https://github.com/gocardless/crank
#

import json

import pytest
import requests
import responses

from gocardless_pro.errors import MalformedResponseError
from gocardless_pro import resources
from gocardless_pro import list_response

from .. import helpers
  

@responses.activate
def test_bank_account_holder_verifications_create():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['create']
    helpers.stub_response(fixture)
    response = helpers.client.bank_account_holder_verifications.create(*fixture['url_params'])
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)
    assert responses.calls[-1].request.headers.get('Idempotency-Key') is not None
    assert response.actual_account_name == body.get('actual_account_name')
    assert response.id == body.get('id')
    assert response.result == body.get('result')
    assert response.status == body.get('status')
    assert response.type == body.get('type')

@responses.activate
def test_bank_account_holder_verifications_create_new_idempotency_key_for_each_call():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['create']
    helpers.stub_response(fixture)
    helpers.client.bank_account_holder_verifications.create(*fixture['url_params'])
    helpers.client.bank_account_holder_verifications.create(*fixture['url_params'])
    assert responses.calls[0].request.headers.get('Idempotency-Key') != responses.calls[1].request.headers.get('Idempotency-Key')

def test_timeout_bank_account_holder_verifications_create_idempotency_conflict():
    create_fixture = helpers.load_fixture('bank_account_holder_verifications')['create']
    get_fixture = helpers.load_fixture('bank_account_holder_verifications')['get']
    with helpers.stub_timeout_then_idempotency_conflict(create_fixture, get_fixture) as rsps:
      response = helpers.client.bank_account_holder_verifications.create(*create_fixture['url_params'])
      assert len(rsps.calls) == 2

    assert isinstance(response, resources.BankAccountHolderVerification)

@responses.activate
def test_timeout_bank_account_holder_verifications_create_retries():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['create']
    with helpers.stub_timeout_then_response(fixture) as rsps:
      response = helpers.client.bank_account_holder_verifications.create(*fixture['url_params'])
      assert len(rsps.calls) == 2
      assert rsps.calls[0].request.headers.get('Idempotency-Key') == rsps.calls[1].request.headers.get('Idempotency-Key')
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)

def test_502_bank_account_holder_verifications_create_retries():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['create']
    with helpers.stub_502_then_response(fixture) as rsps:
      response = helpers.client.bank_account_holder_verifications.create(*fixture['url_params'])
      assert len(rsps.calls) == 2
      assert rsps.calls[0].request.headers.get('Idempotency-Key') == rsps.calls[1].request.headers.get('Idempotency-Key')
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)
  

@responses.activate
def test_bank_account_holder_verifications_get():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['get']
    helpers.stub_response(fixture)
    response = helpers.client.bank_account_holder_verifications.get(*fixture['url_params'])
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)
    assert responses.calls[-1].request.headers.get('Idempotency-Key') is None
    assert response.actual_account_name == body.get('actual_account_name')
    assert response.id == body.get('id')
    assert response.result == body.get('result')
    assert response.status == body.get('status')
    assert response.type == body.get('type')

@responses.activate
def test_timeout_bank_account_holder_verifications_get_retries():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['get']
    with helpers.stub_timeout_then_response(fixture) as rsps:
      response = helpers.client.bank_account_holder_verifications.get(*fixture['url_params'])
      assert len(rsps.calls) == 2
      assert rsps.calls[0].request.headers.get('Idempotency-Key') == rsps.calls[1].request.headers.get('Idempotency-Key')
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)

def test_502_bank_account_holder_verifications_get_retries():
    fixture = helpers.load_fixture('bank_account_holder_verifications')['get']
    with helpers.stub_502_then_response(fixture) as rsps:
      response = helpers.client.bank_account_holder_verifications.get(*fixture['url_params'])
      assert len(rsps.calls) == 2
      assert rsps.calls[0].request.headers.get('Idempotency-Key') == rsps.calls[1].request.headers.get('Idempotency-Key')
    body = fixture['body']['bank_account_holder_verifications']

    assert isinstance(response, resources.BankAccountHolderVerification)
  
