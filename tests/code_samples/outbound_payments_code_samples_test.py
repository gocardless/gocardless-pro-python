# WARNING: Do not edit by hand, this file was generated by Crank:
#
#   https://github.com/gocardless/crank
#

# Code Sample Tests
# These tests verify that the documentation code samples are syntactically valid
# and can execute against a mocked API without errors.
#
# IMPORTANT: These tests do NOT verify business logic - they only verify that
# the code samples compile and execute without syntax errors.

import re
import sys
from io import StringIO

import responses

import gocardless_pro






@responses.activate
def test_outbound_payments_create_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.create(params={
          "amount": 1000,
          "scheme": "faster_payments",
          "description": "Reward Payment (August 2024)",
          "reference": "Invoice 123",
          "links": {
            "creditor": "CR123",
            "recipient_bank_account": "BA123"
          }
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_withdraw_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/withdrawal'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.withdraw(params={
          "amount": 5000,
          "scheme": "faster_payments",
          "description": "Withdraw funds to business account",
          "links": {
            "creditor": "CR123"
          }
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_cancel_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/:identity/actions/cancel'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.cancel("OUT123")
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_approve_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/:identity/actions/approve'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.approve("OUT123")
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_get_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.get("OUT123")
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_list_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': [{}], 'meta': { 'cursors': {'after': None, 'before': None}, 'limit': 50 } }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.list(params={"limit": 10})
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_update_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'PUT',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.update("OUT123", params={
          "metadata": {
            "invoice_id": "INV-1234"
          }
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_outbound_payments_stats_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/outbound_payments/stats'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'outbound_payments': {} }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.outbound_payments.stats()
    finally:
        sys.stdout = old_stdout


