# WARNING: Do not edit by hand, this file was generated by Crank:
#
#   https://github.com/gocardless/crank
#

# Code Sample Tests
# These tests verify that the documentation code samples are syntactically valid
# and can execute against a mocked API without errors.
#
# IMPORTANT: These tests do NOT verify business logic - they only verify that
# the code samples compile and execute without syntax errors.

import re
import sys
from io import StringIO

import responses

import gocardless_pro






@responses.activate
def test_instalment_schedules_create_with_dates_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        instalment_schedule = client.instalment_schedules.create_with_dates(
            params={
                "name": "ACME Invoice 103",
                "total_amount": 10000, # 100 GBP in pence, collected from the customer
                "app_fee": 10, # Your 10 pence fee, applied to each instalment,
                               # to be paid out to you
                "currency": 'GBP',
                "instalments": [
                    {
                        "charge_date": '2019-08-20',
                        "amount": 3400
                    },
                    {
                        "charge_date": '2019-09-03',
                        "amount": 3400
                    },
                    {
                        "charge_date": '2019-09-17',
                        "amount": 3200
                    },
                ],
                "links": {
                    "mandate": 'MD0000XH9A3T4C'
                },
                "metadata": {}
            }, headers={
                'Idempotency-Key': 'random_instalment_schedule_specific_string'
            }
        )
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_instalment_schedules_create_with_schedule_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        instalment_schedule = client.instalment_schedules.create_with_schedule(
            params={
                "name": "ACME Invoice 103",
                "total_amount": 10000, # 100 GBP in pence, collected from the customer
                "app_fee": 10, # Your 10 pence fee, applied to each instalment,
                               # to be paid out to you
                "currency": "GBP",
                "instalments": {
                    "start_date": "2019-08-20",
                    "interval_unit": "weekly",
                    "interval": 2,
                    "amounts": [
                        3400,
                        3400,
                        3200
                    ]
                },
                "links": {
                    "mandate": "MD0000XH9A3T4C"
                },
                "metadata": {}
            }, headers={
                "Idempotency-Key": "random_instalment_schedule_specific_string"
            }
        )
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_instalment_schedules_list_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': [{}], 'meta': { 'cursors': {'after': None, 'before': None}, 'limit': 50 } }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.instalment_schedules.list().records

        client.instalment_schedules.list(params={"after": "IS123"}).records
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_instalment_schedules_get_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': {} }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.instalment_schedules.get("IS123")
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_instalment_schedules_update_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': {} }
    for _ in range(5):
        responses.add(
            'PUT',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.instalment_schedules.update("IS123", params={
          "metadata": {"key": "value"}
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_instalment_schedules_cancel_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/instalment_schedules/:identity/actions/cancel'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'instalment_schedules': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.instalment_schedules.cancel("IS123")
    finally:
        sys.stdout = old_stdout


