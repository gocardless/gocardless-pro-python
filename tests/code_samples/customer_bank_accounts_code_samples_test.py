# WARNING: Do not edit by hand, this file was generated by Crank:
#
#   https://github.com/gocardless/crank
#

# Code Sample Tests
# These tests verify that the documentation code samples are syntactically valid
# and can execute against a mocked API without errors.
#
# IMPORTANT: These tests do NOT verify business logic - they only verify that
# the code samples compile and execute without syntax errors.

import re
import sys
from io import StringIO

import responses

import gocardless_pro






@responses.activate
def test_customer_bank_accounts_create_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/customer_bank_accounts'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'customer_bank_accounts': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.customer_bank_accounts.create(params={
          "account_number": "55779911",
          "branch_code": "200000",
          "account_holder_name": "Frank Osborne",
          "country_code": "GB",
          "links": {
            "customer": "CU123"
          }
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_customer_bank_accounts_list_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/customer_bank_accounts'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'customer_bank_accounts': [{}], 'meta': { 'cursors': {'after': None, 'before': None}, 'limit': 50 } }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.customer_bank_accounts.list(params={"customer": "CU123"}).records
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_customer_bank_accounts_get_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/customer_bank_accounts/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'customer_bank_accounts': {} }
    for _ in range(5):
        responses.add(
            'GET',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.customer_bank_accounts.get("BA123")
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_customer_bank_accounts_update_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/customer_bank_accounts/:identity'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'customer_bank_accounts': {} }
    for _ in range(5):
        responses.add(
            'PUT',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.customer_bank_accounts.update("BA123", params={
          "metadata": {"key": "value"}
        })
    finally:
        sys.stdout = old_stdout




@responses.activate
def test_customer_bank_accounts_disable_code_sample():
    # Convert :param placeholders to regex wildcards for flexible matching
    stub_url = '/customer_bank_accounts/:identity/actions/disable'
    url_pattern = re.compile('https://api.gocardless.com' + re.sub(r':[\w]+', r'[^/]+', stub_url))
    # Mock response - repeat multiple times to handle code samples with multiple API calls
    response_body = { 'customer_bank_accounts': {} }
    for _ in range(5):
        responses.add(
            'POST',
            url_pattern,
            json=response_body,
            status=200
        )

    client = gocardless_pro.Client(access_token='SECRET_TOKEN', environment='live')

    # Suppress stdout from code samples that use print
    old_stdout = sys.stdout
    sys.stdout = StringIO()
    try:
        client.customer_bank_accounts.disable("BA123")
    finally:
        sys.stdout = old_stdout


